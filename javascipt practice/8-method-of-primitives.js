// it all comes at cost.....
// / objects are heavier than primitives

let str ="akcool";

let strClean = str.toLowerCase();

// tn the moment of accessign its property  , a spcl objesct is created that knows that knows the values of the string and 
console.log(strClean);

// strin , number, boolean ,symbol,bigint

